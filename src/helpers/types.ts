export type Keys = {
  left: boolean;
  right: boolean;
  escape: boolean;
  shoot: boolean;
};

export type Direction = "left" | "right" | "none";

export type Coordinates = {
  x: number;
  y: number;
};
